# ToDo List

## DB設計・リポジトリ・ユースケース・DTO・マッパー関連

- Androidウィジェット用に対象グループの旅行と旅程を取得するユースケースを作成する
  - 対象グループの直近の旅行を判定する
  - 直近の定義（旅行期間中、次回予定、直近の過去旅行の優先順）を実装前に確定する
  - 左右ボタンで切り替えるため、直近前後の旅行もキャッシュ対象に含める

## マップの表示


## トップ画面


## アカウント管理


## グループ管理


## メンバー管理画面

## グループ管理画面

## 設定画面

- 設定画面でAndroidウィジェットの表示対象グループを設定できるようにする
  - 表示対象グループは端末ごとに1件に制限する
  - 表示対象を解除できるようにする
  - 設定はDBのgroupsではなく、端末ローカルの保存領域に保持する

## Androidウィジェット

- Androidホーム画面ウィジェットで対象グループの直近の旅程を表示する
  - AndroidネイティブのAppWidgetProviderまたはFlutter連携パッケージの採用方針を決める
  - 旅行名、旅行期間、旅程項目名、開始日時、終了日時、メモを表示対象にする
  - 旅程項目は開始日時順、開始日時が同じ場合は終了日時順で表示する
- Androidウィジェットに左右ボタンを追加し、キャッシュ済みの別旅行へ切り替えられるようにする
- Androidウィジェットに更新ボタンを追加し、更新ボタンを押した場合のみDBから最新データを取得する
  - 通常表示と左右切り替えではDB通信せず、保存済みキャッシュを表示する
  - 更新成功時はキャッシュとウィジェット表示を更新する
  - 更新失敗時はキャッシュ済みの内容を維持し、最終更新日時またはエラー状態を表示する
- Androidウィジェット用のキャッシュ保存先と保存形式を決める
  - Flutter側とAndroidネイティブ側の両方から参照できる形式にする
  - ログアウト時や対象グループ変更時に古いキャッシュを削除する
  - 対象グループIDも端末ローカルの設定としてキャッシュと同じ管理範囲で扱う


## グループ年表画面

## 旅行管理画面

## マップピンボトムシート

## 招待機能

## グループイベント

## メンバーイベント

## DVCポイント計算画面

## デザイン

## 全体

## リファクタリング

## 不具合修正
