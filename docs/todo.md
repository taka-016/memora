# ToDo List

## DB設計・リポジトリ・ユースケース・DTO・マッパー関連

## マップの表示


## トップ画面


## アカウント管理


## グループ管理


## メンバー管理画面

## グループ管理画面


## グループ年表画面

## 旅行管理画面

## マップピンボトムシート

## 招待機能

## グループイベント

## メンバーイベント

## DVCポイント計算画面

## デザイン

## 全体

- アプリ内の現在時刻取得を端末依存の`DateTime.now()`から共通の時刻取得サービスへ置き換える
  - NTPまたはサーバー基準時刻を取得する実装を用意する
  - ユースケースや表示ロジックでは直接`DateTime.now()`を呼ばず、共通サービス経由で現在時刻を取得する
  - テストでは固定時刻を返す実装に差し替えられるようにする
- 時刻情報は永続化・内部処理ともにUTCで保持する方針へ統一する
  - Firestoreへ保存している日時フィールドを全テーブル分洗い出し、UTC保持対象フィールド一覧を作成する
  - 共通の時刻取得サービスはUTCの`DateTime`を返す
  - DTO、エンティティ、FirestoreMapperで扱うDateTimeはUTCであることを前提にし、境界でUTCでない値が入らないように検証する
  - Firestoreへ保存するDateTimeはUTCであることをテストで保証する
  - DTOとエンティティのDateTimeフィールドはUTC前提で受け渡すようにテストを追加する
  - 有効期限や日付範囲の比較処理はUTC同士で比較するように修正する
  - 表示時のみ設定タイムゾーンへ変換する
  - 現行テーブルの時刻データをUTC基準へ変換する移行スクリプトを用意する
  - 移行前後の件数、対象フィールド、変換結果を検証できるドライラン手順を用意する
- DatePickerで選択する日付のみの値は、共通の日付型・変換処理で扱う
  - 誕生日、旅行開始日、旅行終了日、年月など、時刻を意味しないフィールドを洗い出す
  - 日付のみの値は日時のUTC比較対象とは別カテゴリとして扱う
  - DatePickerの戻り値を日付のみの共通型または共通変換関数へ通し、UTC時刻としてそのまま保存しない
  - 日付のみのFirestore保存、DTO・エンティティ変換、表示変換を共通ヘルパーに集約する
  - 個別画面で独自の日付変換ロジックを持たないようにする
  - 表示タイムゾーン変更時にも日付のみの値が前日・翌日にずれないことをテストする
- 設定画面でアプリ内のタイムゾーンを変更できるようにする
  - 設定画面にタイムゾーン選択UIを追加する
  - 選択したタイムゾーンIDを永続化する
  - 日付表示処理でUTCのDateTimeを設定タイムゾーンへ変換して表示する
  - 有効期限判定などの内部比較処理では設定タイムゾーンを使用しない
  - 設定したタイムゾーンを永続化し、アプリ再起動後も反映する

## リファクタリング

## 不具合修正
