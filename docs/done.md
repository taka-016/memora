# Done List

## DB設計・リポジトリ・ユースケース・DTO・マッパー関連

- pinsテーブルにlocationNameカラムを追加する
- group_membersにisAdministratorを追加する
- group_membersにorderIndexを追加する
- GroupWithMembersDtoのマッパーを作成する
- GroupWithMembersMapperにtoEntityとtoEntityListを追加する
- GroupWithMembersDtoにownerIdを追加する
- GroupRepositoryにgetGroupsWithMembersByMemberIdメソッドを追加する
- GetGroupsWithMembersUsecaseをリファクタリングしてリポジトリの単一メソッドを使用する
- GetManagedGroupsWithMembersUsecaseをリファクタリングしてリポジトリの単一メソッドを使用する
- GroupをルートエンティティとしてGroupMemberを内部エンティティに加える
- TripEntryの集約に訪問場所と詳細予定を追加しリポジトリを対応させる
  - getTripEntries→getTripEntriesByGroupIdに変更
  - getで子エンティティも取得する
  - save,update,deleteも子エンティティに対応する
- GetPinsByTripIdUseCaseの使用を廃止し、GetTripEntriesUsecaseで取得したpinsデータを使用する
- FirestorePinQueryServiceを作成し、自分が所属するグループに紐づくピンを取得する
- TripEntryRepositoryのgetTripEntryByIdにpinsとpinDetails用のorderByを設定できるようにする
- MemberRepositoryのgetMembersとgetMembersByOwnerIdにorderByを追加する
  - GetManagedMembersUsecaseでdisplayNameの昇順で取得する処理を追加する
- GroupRepositoryのgetGroups、getGroupsByOwnerId、getGroupByIdにorderByを追加する
  - getGroups、getGroupsByOwnerIdはgroupsのソートのためのorderBy、getGroupByIdはmembersのソートのためのorderBy
- FirestoreGroupQueryServiceのgetGroupsWithMembersByMemberIdにorderByを追加する
  - groupsのソートのためのorderBy、membersのソートのためのorderByを追加する
  - GetGroupsWithMembersUsecaseでgroupsのnameの昇順とmembersのdisplayNameの昇順で取得する処理を追加する
- FirestoreGroupQueryServiceのgetManagedGroupsWithMembersByOwnerIdにorderByを追加する
  - groupsのソートのためのorderBy、membersのソートのためのorderByを追加する
  - GetManagedGroupsWithMembersUsecaseでgroupsのnameの昇順とmembersのdisplayNameの昇順で取得する処理を追加する
- getGroups、getGroupsByOwnerIdは使用していないため廃止する
- getGroupByIdのorderByはgroup_memberではソートキーとなる要素がないため廃止する
- FirestoreGroupQueryServiceにgetGroupWithMembersByIdを追加する
  - グループIDでgroupsを取得し、その子エンティティであるgroup_membersを紐づけmember_idでmembersも紐づけて取得する
  - 取得結果はGroupWithMembersDtoに格納する
- GroupDtoをEquatable化して、copyWithも追加する
- TripEntryRepositoryのgetTripEntryById,getTripEntriesByGroupId,getTripEntriesByGroupIdAndYearをTripEntryQueryServiceに実装する（段階的に移行するので、この時点ではTripEntryRepositoryにも残しておく）
- MemberRepositoryのgetMembers,getMemberById,getMemberByAccountId,getMembersByOwnerIdをMemberQueryServiceに実装する（段階的に移行するので、この時点ではMemberRepositoryにも残しておく）
- MemberInvitationRepositoryのgetByInviteeId,getByInvitationCodeをMemberInvitationQueryServiceに実装する（段階的に移行するので、この時点ではMemberInvitationRepositoryにも残しておく）
- TripEntryDtoを作成する
- PinDetailDtoを作成する
- MemberInvitationDtoを作成する
- GroupEventDtoを作成する
- MemberEventDtoを作成する
- TripEntryMapperを作成する
- PinDetailMapperを作成する
- MemberInvitationMapperを作成する
- GroupEventMapperを作成する
- MemberEventMapperを作成する
- FirestoreMemberQueryServiceのレスポンスはDtoを返すように変更する
  - getMembersの戻り値をMemberDtoに変更する
  - getMemberByIdの戻り値をMemberDtoに変更する
  - getMemberByAccountIdの戻り値をMemberDtoに変更する
  - getMembersByOwnerIdの戻り値をMemberDtoに変更する
- TripEntryQueryServiceのレスポンスはDtoを返すように変更する
  - getTripEntryByIdの戻り値をTripEntryDtoに変更する
  - getTripEntriesByGroupIdAndYearの戻り値をTripEntryDtoに変更する
- MemberInvitationQueryServiceのレスポンスはDtoを返すように変更する
  - getByInviteeIdの戻り値をMemberInvitationDtoに変更する
  - getByInvitationCodeの戻り値をMemberInvitationDtoに変更する
- ER図のroutesテーブル定義を元にRouteエンティティを作成する
  - lib/domain/entities/trip/route.dartの作成
  - lib/application/dtos/trip/route_dto.dartの作成
  - lib/domain/repositories/trip/route_repository.dartの作成
  - lib/application/queries/trip/route_query_service.dartの作成
  - lib/infrastructure/repositories/trip/firestore_route_repository.dartの作成
  - lib/infrastructure/queries/trip/firestore_route_query_service.dartの作成
  - lib/infrastructure/mappers/trip/route_mapper.dartの作成
  - lib/application/mappers/trip/route_mapper.dartの作成
- RouteエンティティはTripEntryエンティティの集約に加える
  - TripEntryRepository、TripEntryQueryService、FirestoreTripEntryRepository、FirestoreTripEntryQueryServiceを対応させる
  - TripEntryMapper、TripEntryDtoを対応させる
  - Routeエンティティクラスにidは含めない（関連処理もあわせて修正する）
  - RouteRepositoryを廃止し、関連実装・テスト・DIから除去する
- ER図のtasksテーブル定義を元にTaskエンティティを作成する
  - Taskエンティティにidは含めない
  - TaskQueryService、FirestoreTaskQueryServiceを作成する
  - TaskDtoとTaskMapperを作成する
- TaskエンティティはTripEntryエンティティの集約に加える
  - TripEntryRepository、TripEntryQueryService、FirestoreTripEntryRepository、FirestoreTripEntryQueryServiceを対応させる
  - TripEntryMapper、TripEntryDtoを対応させる
- trip_entryのエンティティのtaskのエラーチェックから、orderindexの重複チェックを廃止する
- 存在しない親タスクidが設定されている場合、エラーとする(タスクidの検索範囲は、trip_entryに紐づくタスク内)
- DVC関連テーブルを実装する
  - dvc_point_contractsテーブルを実装する
    - ER図どおりにカラムを定義する
    - エンティティ・DTO・Mapperを作成する
    - Repository・QueryService・Firestore実装を作成する
  - dvc_limited_pointsテーブルを実装する
    - ER図どおりにカラムを定義する
    - エンティティ・DTO・Mapperを作成する
    - Repository・QueryService・Firestore実装を作成する
  - dvc_point_usagesテーブルを実装する
    - ER図どおりにカラムを定義する
    - エンティティ・DTO・Mapperを作成する
    - Repository・QueryService・Firestore実装を作成する

## マップの表示

- ~~起動時に現在地に移動~~→仕様変更により廃止
- 地図表示画面はMapViewを直接表示するのではなく、mapDisplayウィジェットを表示してその上にMapViewを生成する形にする
- PinQueryServiceのgetPinsByMemberIdを使用してログインユーザー(ユーザーIDに紐づくmember)が所属するグループに紐づくpinsを取得する
  - 取得したpinsをマップにmarkerで表示する
  - markerタップでpin内容を表示するが、変更/削除は不可とする
- ~~ピンをタップでポップアップメニュー表示~~→仕様変更により廃止
  - ~~「削除」メニューの追加~~
- ピン位置の保存
  - ピン位置をFirebaseに保存する処理
  - 保存成功・失敗時のUI反映
  - UUID、ピン位置の保存
- マップ起動時に保存済みのピンをマップに表示
  - Firebaseからピン位置リストを取得
  - ピンリストをマップ上に配置
- 削除ボタンタップ時にピン位置を削除
  - Firebaseから該当ピンを削除する処理
  - マップ上から該当ピンを削除
  - markerIdで紐づけて削除する
- 汎用検索バーWidgetを作成する
- 検索バーをマップ上に配置
- 検索バーに入力されたキーワードで位置検索
- 検索結果の位置に地図を移動
- ~~ピンをタップしたときに詳細入力画面をモーダル表示~~→仕様変更によりボトムシート表示に変更
- 詳細入力画面のUIを作成
  - 旅行期間From、旅行期間Toの入力フィールド
  - メモの入力フィールド（旅の記録というラベルで）

## トップ画面

- Googleマップ画面は別画面でも使用するため、ウィジェット化する
  - google_map_screenはウィジェットを表示する形にする
- トップページを作成する。
  - トップにはグループの情報が表示される
  - グループが複数件ある場合は、グループ一覧が表示される
  - グループが1件しかない場合は、グループ一覧ではなくグループのメンバー一覧が表示される
  - グループが存在しない場合は、グループ作成ボタンが表示される
  - グループを選択すると、そのグループ内のメンバー一覧が表示される
  - グループにメンバーが存在しない場合は、メンバー追加ボタンが表示される
- アプリ起動時にトップページを表示する
- HomePageは廃止する
- トップページの構成を変更
  - トップページの左上にハンバーガーメニューを追加する
  - 常にトップページが表示され、メニューで選択された機能のウィジェットが表示されるようにする
  - メニュー項目はトップページ、グループ年表、マップ表示、グループ設定、メンバー設定、設定とする（トップページが初期表示のグループ情報のこと）
- GroupMemberウィジェットの改修
  - 変更：グループが複数件ある場合は、グループ一覧が表示される→グループが1件でもグループ一覧を表示する
  - 廃止：グループが1件しかない場合は、グループ一覧ではなくグループのメンバー一覧が表示される
  - 廃止：グループにメンバーが存在しない場合は、メンバー追加ボタンが表示される
  - 廃止：グループを選択すると、そのグループ内のメンバー一覧が表示される
  - 変更：グループが存在しない場合は、グループ作成ボタンが表示される→「グループがありません」のラベル表示のみ
  - 変更：トップページというメニューは廃止し、グループ年表のメニューからGroupMemberに遷移する
- 並び替え：メニューの並びを「メンバー設定」と「グループ設定」の順番を入れ替える
- 画面遷移制御をコントローラーに分離する
  - NavigationNotifierクラスを作成する（lib/application/controllers/navigation_controller.dart）
  - GroupTimelineNavigationNotifierクラスを作成する（lib/application/controllers/group_timeline_navigation_controller.dart）
  - TopPageをリファクタリングしてコントローラーを使用する形に変更する
- トップページのヘッダ色をハンバーガーメニューのヘッダ色と合わせる
- _currentMember取得でエラーになった場合、showSnackBarでエラーを表示し、そのままログアウトする

## アカウント管理

- アカウント管理はGoogle Cloud Identity Platformを使用する
  - 認証関連のエンティティ作成（User, AuthState等）
  - 認証サービス抽象化インターフェース作成
  - Firebase Auth実装クラス作成
  - 認証ユースケース実装（ログイン、ログアウト、サインアップ等）
  - 認証状態管理マネージャー作成
  - ログイン・サインアップ画面UI実装
  - 認証ガード機能実装（未認証時のリダイレクト処理）
- ログイン成功時に、アカウントのUIDでmembersのaccountIdを紐づけて取得する
  - UIDでmembersが紐づかなかった場合、membersを新規作成しaccountIdにUIDを保持させる
  - GetOrCreateMemberUseCaseは戻り値をbooleanにする
  - AuthNotifierでGetOrCreateMemberUseCaseの戻り値を見る（Falseの場合、強制ログアウト）
- ログアウトボタンはメニューの最下部に配置する
- ログインユーザーのニックネームをメニューの上部に表示する
- ログインユーザーのニックネームが未設定の場合は、kanjiLastName+半角スペース+kanjiFirstNameを表示する
- ログインユーザーのkanjiLastNameとkanjiFirstNameが両方未設定の場合は、"名前未設定"と表示する
- 仕様変更：ニックネーム→表示名という必須フィールドに変更したため、常にログインユーザーの表示名を表示する
- 仕様変更：メニューの上部に表示する表示名は、ログインIDに変更する
- アカウント設定画面を作成する
  - メニューの「アカウント設定」から遷移できるようにする
  - メールアドレスの変更機能
  - パスワードの変更機能
  - アカウント削除機能
- パスワードポリシーを大文字、小文字、特殊文字、数字が必要かつ最低8文字以上とする
- firestoreのローカルキャッシュを無効化する
- トークンの有効期限が切れている場合はログイン画面に戻す
- アカウント作成画面のパスワード要件に、特殊文字で使用できる文字を明記する
- AccountDeleteModalの削除処理をAccountSettingsで実行する

## グループ管理

- GroupをownerIdで抽出する処理をリポジトリに追加する
- GroupMemberをmemberIdで抽出する処理をリポジトリに追加する
- GetGroupsWithMembersUsecaseのexecuteメソッドを修正する
  - memberを引数に取るようにする
  - Group取得はgetGroupsは使用せず、getGroupsByOwnerIdを使用する（member.idを使用）
  - Groupは以下の結果もマージする
    - GroupMemberをgetGroupMembersByMemberIdで取得（member.idを使用）
    - GroupをgetGroupsByGroupIdで取得（groupMember.groupIdを使用）
  - GroupMember取得はgetGroupMembersは使用せず、getGroupMembersByGroupIdを使用する（getGroupsの結果で紐づける）
  - Members取得はgetMembersは使用せず、getMemberByIdを使用する（getGroupMembersの結果で紐づける）
- GroupMemberの修正
  - GroupMemberはmemberを引数に取るようにする
  - getGroupsWithMembersUsecase.executeにmemberを渡す
  - topPageからGroupMemberにmember(ログインユーザーに紐づくmember)を渡す
- グループオーナーは必ずグループメンバーに含める
  - グループ新規画面のメンバー一覧の先頭にログインユーザーを固定表示し、新規/編集モードともに削除不可かつ管理者固定で変更不可（＝アクションメニューのボタンを表示しない）
- グループメンバーはオーナー含め並び替え可能とする
  - 現状はオーナーが必ず先頭に来る仕組みになっているが、これを廃止し、リスト内にオーナーがいればOKとしてorderIndexを優先する

## メンバー管理画面

- メンバー設定メニューから開く
- メンバー一覧表示
  - ログインユーザーが管理しているメンバーの一覧を表示する
  - ログインユーザーのmemberIdでownerIdを紐づけて取得する
  - 1行目にログインユーザーに紐づくmember(TopPageの_currentMember)を表示する（削除不可とする）
  - ログインユーザー行の表示情報を他の行と同様に調整する
  - ログインユーザー行の削除ボタンを使用不可から非表示に変更する
  - 必ず1行以上存在するため、空状態メッセージの処理は不要
  - 編集ボタンではなく、行タップで編集画面に遷移するように変更
- メンバーのメールアドレスがブランクの場合、メンバー一覧のカードにメールアドレスは表示しない
- メンバー新規登録
  - Memberのid,accountId,ownerId以外の入力項目を作成する(モーダル画面)
  - 登録時にログインユーザーのmemberIdをownerIdにセットする
- メンバー情報編集
  - メンバー一覧から対象メンバーの編集ボタンをクリックして開く
  - メンバー新規登録と同一の情報がすべて編集可能（同一画面を使いまわす）
  - 1行目のログインユーザーに紐づくメンバーを編集・更新後、反映する
    - パラメータのmemberをそのまま使用せず、memberのidでDBから再取得する
    - GetMemberUseCaseを使用する
- メンバー削除
  - メンバー一覧から対象メンバーの削除ボタンをクリック
  - 確認ダイアログ(モーダル)を表示して、OKなら削除実行
- newMemberの作成はcreateMemberUsecase側の責務とし、executeにeditedMemberとownerIdを渡す形に変更する
- accountIdを持っているメンバーは削除不可とする（削除ボタンを表示しない）
- メンバー削除時は、memberIdで紐づくテーブル（trip_participants,group_members,member_events）をすべて削除する（delete_member_usecaseで対応）
- メール認証の有効化
- 生年月日は未来日付も入力可能にする

## グループ管理画面

- グループ設定メニューから開く
- usecaseの作成
  - GroupをownerIdで取得するusecaseを作成する（GetManagedMembersUsecaseを参考に）
  - GroupMemberをgroupIdで取得するusecaseを作成する（GetGroupMembersByGroupIdUsecase）
  - Groupを新規作成するusecaseを作成する（CreateMemberUsecaseを参考に）
  - Groupを編集するusecaseを作成する（UpdateMemberUsecaseを参考に）
  - Groupを削除するusecaseを作成する（DeleteMemberUsecaseを参考に）
- グループ一覧表示(MemberManagementを参考に)
  - ログインユーザーが管理しているグループの一覧を表示する
  - ログインユーザーのmemberIdでownerIdを紐づけて取得する
- グループ新規登録(MemberManagement,MemberEditModalを参考に)
  - Groupのid,ownerId以外の入力項目を作成する(モーダル画面)
  - 登録時にログインユーザーのmemberIdをownerIdにセットする
  - グループに所属させるメンバーを選択できる（ログインユーザーが管理しているメンバーから選択）
  - メンバー選択の対象数が多い場合、メンバー一覧のみをスクロールする
- グループ情報編集(MemberManagement,MemberEditModalを参考に)
  - グループ一覧から対象グループの行をクリックして編集する
  - グループ新規登録と同一の情報がすべて編集可能（同一画面を使いまわす）
  - グループに所属させるメンバーの追加削除ができる（ログインユーザーが管理しているメンバーから選択）
- GetManagedGroupsUsecaseをGetManagedGroupsWithMembersUsecaseに改修
  - GetManagedGroupsUsecaseにメンバー情報も取得する機能を追加
  - ファイル名とクラス名をget_managed_groups_with_members_usecaseに変更
  - グループ設定画面でGetManagedGroupsWithMembersUsecaseを使用
- _showGroupEditModalの既存グループメンバー取得処理を削除
  - GetManagedGroupsWithMembersUsecaseで既にメンバー情報を取得しているため、重複する処理を削除
  - 編集モーダルには内部で保持しているメンバー情報を渡すように変更
- グループメンバー更新時の効率化
  - DeleteGroupMembersByGroupIdUsecaseを作成
  - 既存のGroupMember削除処理で_getGroupMembersByGroupIdUsecaseの重複呼び出しを削除
  - 新しいユースケースを使用してグループのすべてのメンバーを一括削除
- グループ削除(MemberManagementを参考に)
  - グループ一覧から対象グループの削除ボタンをクリック
  - 確認ダイアログ(モーダル)を表示して、OKなら削除実行
  - グループに紐づくグループメンバーもあわせて削除する
- グループ新規作成時にグループメンバーを作成した場合、group_membersのgroupIdにグループのidが入っていない不具合を修正（idを統一する必要がある）
- グループに紐づくグループメンバーの削除は_deleteGroupUsecase.executeの責務とする
- グループ削除時は、groupIdで紐づくテーブル（group_members,group_events,trip_entries）をすべて削除する（delete_group_usecaseで対応）
- グループ削除時は、groupIdで紐づくtrip_entriesが削除されるため、tripIdで紐づくpinsとtrip_participantsも削除する（delete_group_usecaseで対応）
- グループ編集モーダルのメンバー追加・変更UIをプルダウン選択に変更する
- グループ編集モーダルでメンバー選択後にフォーカスを外す
- グループ編集モーダルのメンバー一覧表示を操作メニュー付きで改善する
- メンバー一覧で管理者を設定可能にする
- GroupEditModalのgroupをGroupDtoに変更する
  - GroupManagementの_showEditGroupDialogは_getGroupByIdUsecaseを使用せず、GroupEditModalのgroupにgroupWithMembersをそのまま渡す
  - GroupMemberではなくGroupMemberDtoを使用する
  - onSaveに渡す際にGroupMapperのtoEntityでGroupエンティティを渡す

## グループ年表画面

- グループ一覧画面→グループ年表画面の順に遷移する
- グループ年表の枠を作成
  - 列：年(2025年(令和7年) というフォーマット)
  - 行：旅行、イベント、グループ一覧より渡されたグループメンバーを1人1行ずつで表示する
  - 現在の年を中央として、前後5年分の年を表示する
  - 表示している年以上を表示したい場合、末尾と先頭に配置された「さらに表示する」をタップすると、さらに5年分の年を表示する（末尾の場合はー5年、先頭の場合は+5年）
  - 初期表示時は現在の年を中央に表示する
    - 年表の幅から中央位置を計算し、現在の年が中央に来るようにスクロールする
  - 行の1列目はスクロールせず左端に固定する
  - 列の区切り線を表示する
  - スクロールテーブルの行の高さをドラッグで変更できるようにする
- 左上に戻るアイコンを設置し、タップでグループ一覧画面に戻る
- 旅行のセルをタップすると、旅行管理画面（モーダル）を開く
- 仕様変更：旅行管理画面はモーダルではなくウィジェットにする
  - 一覧→作成/編集と遷移するので今のモーダルは完全に捨てる
  - 旅行管理画面に、タップしたセルの年とグループIDを渡す
- 遷移先画面から戻った場合、遷移前のインスタンスを再利用する
- groupTimelineの旅行行に、対象年の旅行を一覧表示する
  - nameがある場合：「tripStartDateのyyyy/mm/dd \n name」形式
  - nameがない場合：「tripStartDateのyyyy/mm/dd \n 旅行名未設定」形式
  - 表示しきれない場合の省略処理
  - 行の高さに応じた表示内容の増減
- 遷移先画面から戻った場合、旅行行の内容を更新する
- グループメンバー行の各カラムの1行目に、年齢を表示する
  - 年齢はその年の12月31日時点での年齢とする
  - 生年月日が登録されていない場合は空白とする
- グループメンバー行の各カラムの2行目に、保育所新生児クラス～大学4年生までの学年を表示する
  - 学年はその年の3月31日時点での年齢で計算する
  - 表示ラベルは「新生児/0歳児/1歳児/2歳児/年少/…/高校3年生/大学4年生」とする
  （生まれた年が新生児で、生まれた日が3/31以前なら新生児/0歳児と併記）
  - 範囲外は空白とする
  - 生年月日が登録されていない場合は空白とする
  - 前行が空白の場合は詰めて表示する
  - 学年の計算ロジックをユースケースに切り出す
- グループメンバー行の各カラムの3行目に、厄年を表示する
  - 厄年は数え年で計算する
  - 数え年は生まれた時が1歳で1月1日（元日）を迎えるごとに1歳年を取る
  - 「前厄/本厄/後厄」まで表示する
  - gender が「その他」や null の場合は空白とする
  - 生年月日が登録されていない場合は空白とする
  - 前行が空白の場合は詰めて表示する
  - 厄年の計算ロジックをユースケースに切り出す
- 画面右上に設定アイコンを表示し、タップで年齢・学年・厄年の表示/非表示の切り替えができるようにする
  - 表示/非表示の設定はローカルストレージに保存し、次回以降も保持する

## 旅行管理画面

- 旅行一覧表示
  - toppage内のウィジェットとして表示する
  - タイトル、追加ボタン、リストのデザインをグループ管理画面と同様にする
  - パラメータで受け取ったグループIDと年でtrip_entriesを抽出し、一覧表示する
- 旅行のメモがブランクの場合、旅行一覧のカードにメモは表示しない
- 旅行追加ボタンを押すと、旅行作成画面（trip_edit_modal）をモーダルで表示する
- 旅行作成画面
  - trip_entriesの新規作成および編集で使用する
  - 旅行期間From、旅行期間Toの入力フィールド
  - メモの入力フィールド
  - DB書き込みボタンの名称を新規作成時は「作成」、編集時は「更新」と表示する
- member_managementとmember_edit_modalの構造を参考にし、データの準備までをモーダル画面で行う訪問場所の入力機能を実装する
- 訪問場所の入力
  - メモの下に訪問場所の入力フィールドを追加する
  - 訪問場所の入力フィールドの右に地図アイコンを配置する
  - 地図アイコンをタップでモーダル画面上にMapDisplayを表示する
- 仕様変更：「訪問場所を地図で選択」をタップでモーダル画面上にMapDisplayを表示する
- 開始日と終了日がパラメータの年と異なる場合、エラーを表示する
- 年またぎ旅行への対応（終了日の年チェックを削除）
- パラメータの年と現在年が異なる場合、カレンダーの初期表示はパラメータの年の1月にする
- 開始日が入力されている場合かつ終了日が未入力の場合、終了日タップで開くカレンダーは開始日の年月を初期値とする
- 新規作成時のマップピン操作
  - pinsは初期値状態（空のデータ）
  - マップ上のマーカーとpinsを同期する（このタイミングはDBに書き込まない）
  - 登録ボタン実行時に、旅行管理画面でtrip_entriesの登録と合わせてtripIdをキーとしてpinsを登録する
- 編集時のマップピン操作
  - 旅行管理画面でtrip_entriesのidでpinsのtripIdを紐づけて取得し、旅行編集画面に渡し、pinsにセットする
  - マップ上のマーカーとpinsを同期する（このタイミングはDBに書き込まない）
  - 更新ボタン実行時に、旅行管理画面でtrip_entriesの更新と合わせてtripIdをキーにpinsをDelete&insertする
- trip_entries削除時は、tripIdで紐づくpinsとtrip_participantsを削除する（delete_trip_usecaseで対応）
- onPinSavedを作成する
  - 地図画面のonMarkerSavedに渡す
  - pinを受け取り、_pinsから同じpinIdのデータを抽出して更新する
- 「訪問場所を地図で選択」の下に訪問場所の一覧をリスト表示する
  - リストのデザインは旅行管理画面の旅行一覧と同様にする（モーダル画面なので少し小さく）
  - pinsのlocationName、visitStartDate、visitEndDateを表示する
  - locationNameが空の場合は空白表示とする
  - リストタップでボトムシートを開き、内容を編集できるようにする
  - 削除ボタンを配置し、タップで該当ピンを削除する
  - 訪問場所の一覧はvisitStartDateの昇順でソートして取得する（リポジトリにソート処理追加）
- 「訪問場所を地図で選択」のボタン名を「地図で選択」に変更する
- 旅行一覧はtripStartDateの昇順でソートする
- pinエンティティを直接使用しない
- pinsにgroupIdを保持する
- 「経路情報」ボタンの追加
  - 旅行編集画面に「経路情報」ボタンを追加する
  - ボタンをタップで経路情報ダイアログを表示する（新規ウィジェットとしてlib/presentation/shared/dialogs以下に作る）
  - pinのリストが並んでおり、順番をドラッグで入れ替えられる
  - 各pin間はスペースが開いて下矢印でつながっており、矢印に右に移動手段のプルダウンがある
    - プルダウンは、自動車(DRIVE)、徒歩(WALK)、~~公共交通機関(TRANSIT)~~が選択可能で、初期値は自動車(DRIVE)
    - 表示領域が限られているので、プルダウン以外の余計な情報を表示しない
    - プルダウンにその他(OTHER)を追加し、選択時にプルダウンの右にテキスト入力欄を表示して自由入力できるようにする
    - プルダウンにその他(OTHER)を選択した箇所はAPIを呼び出さず、Polylineは前Pinの位置から後Pinの位置までを直線で結ぶ
  - Pinが多い場合はスクロールできるようにする
  - PinはlocationNameのみ表示し、visitStartDate、visitEndDateは表示しない
  - ダイアログ上部に「経路検索」ボタンを配置し、タップでGoogleのRoutes APIを使用して経路情報を取得する
    - 出発時間の指定は不要
    - 中間地点は使用せず、各pin間の経路に設定された移動手段で1回ずつAPIを呼び出して取得する
    - つまり、pinが3つある場合は、pin1→pin2、pin2→pin3の2回APIを呼び出す
- ダイアログ画面の下半分にGoogleMapを表示し、取得した経路情報をMap上に表示する
  - 各Pinをタップで、GoogleMap上の対象となる経路をハイライト表示する
  - GoogleMapはmap_viewsは使用せず、このダイアログ用にMapを生成する
  - +/-アイコンでGoogleMapは表示/非表示を切り替えられるようにする
  - 経路ごとに異なる色でPolylineを表示する（最大10色用意し、11個目の経路以降は1色目から再利用する）
  - Pinの下に経路詳細を表示する
    - DropdownButtonの右に表示する
    - Pin間の距離(km)と所要時間(分)、経路案内を表示する
    - 距離が設定されていない場合は、所要時間(分)のみ表示する
    - 経路詳細は折りたたみ表示とし、タップで展開/折りたたみできるようにする
    - 経路詳細のボリュームに合わせてPin間の高さが変わるようにする
    - 仕様変更：アコーディオンボタンには「ルートメモ」と表示し、距離、所要時間、経路案内は展開した中のメモとして表示する
    - 「ルートメモ」は検索有無にかかわらず表示する
- 旅行編集画面の経路検索を「地図で選択」と同じダイアログ内の表示切り替えに変更し、ウィジェット名からdialogを外す
- 「地図で選択」ボタンを「訪問場所を選択」に名称変更する
- 旅行編集画面の「訪問場所を選択」の表示処理をselect_visit_location_view.dartに切り出す
  - _buildSelectVisitLocationExpandedLayoutが_buildRouteInfoExpandedLayoutと同様にreturn SelectVisitLocationViewになるようにする
- 自由入力の入力欄を廃止し、「その他」選択時は右に経路入力用のアイコンを配置する（Editアイコン）
- 経路入力用のアイコンをタップすると、経路入力用のボトムシートが表示されるようにする
  - ボトムシートはlib/presentation/shared/sheets以下に新規作成し、名称はother_route_info_bottom_sheet.dartとする
- ボトムシート内の項目は以下のとおり
  - 所要時間(分)の入力欄（数字のみ）
  - 経路案内の入力欄（複数行テキスト）
  - 保存ボタンは用意せず、入力欄のフォーカスが外れたタイミングで内部保持する
  - 入力内容は、「その他」で経路検索した時の経路情報に表示する
  - ボトムシートを閉じると、即時にルートメモに反映される（検索の有無にかかわらず表示）
- 「訪問場所を選択」と「経路情報」ボタンの配置を横並びにする
- 「訪問場所を選択」ボタンの名称を「編集」に変更する
- 「編集」と「経路情報」ボタンにアイコンを追加する
- RouteMemoEditBottomSheetで入力した内容はRouteMemoEditFormValueではなく、
  - _searchRoutesでotherの場合はRouteMemoEditBottomSheetでRouteSegmentDetailを作成しているため、
  polylineの内容のみマージする
- 経路マップを閉じている場合に「経路検索」を実行するとエラーが発生する不具合を修正
- 子タスクの開閉ボタンの領域はあらかじめ確保しておき、子タスクの有無で親タスク名の位置が右にずれないようにする
- チェックボックスとタスク名の間が広く空いているため、詰める
- trip_entryのtripStartDate(旅行開始日)とtripEndDate(旅行終了日)は任意入力とする
  - trip_entryにtripYear("NOT NULL")を追加し、年での紐づけはすべてtripYearで行う
  - エンティティ、リポジトリ、ユースケース、マッパー、DTO、画面の修正
  - エンティティでのpinの訪問開始日、訪問終了日のチェックはtripStartDate、tripEndDateが存在する場合は日付を含めてチェックし、存在しない場合は年のみでチェックする
- 旅行編集画面の旅行開始日、旅行終了日をクリア可能にする
  - CopyWithでnull代入を可能にする必要あり
- 旅行編集画面にタスク管理機能を追加する
  - trip_edit_modalの訪問場所より上部の位置に「タスク管理」ボタンを追加する
  - select_visit_location_viewやroute_info_viewと同様に同一ダイアログ内の表示切り替えで遷移できるようにする
  - TaskViewウィジェットを新設し、expandedSectionに追加して切り替え表示できるようにする
  - タスクはリスト形式で表示し、カード上のチェックボックスでタスクの完了ができるようにする
  - 上部にタスク名の入力欄があり、入力して「追加」ボタンタップするとリストに追加される
  - リストのカード上に削除ボタンを配置する
  - リストのカード上のチェックボックスと削除ボタン以外の箇所をタップでボトムシートが表示され、親タスク、担当者、締切日、メモを入力できる(ボトムシートは別ソースファイルで作成する)
  - 締切日はcustom_date_pickerで入力する
  - 担当者はグループメンバーから選択する
  - 親子タスクの階層は2階層までとするため、既に親タスクがあるタスクは、他のタスクの親タスクとして指定できない
  - 子タスクがあるタスクは親タスクを指定できない
  - 親タスクが指定されている場合、親タスクの下に少しインデント下げる形で表示する(開閉形式で非表示にできる)
  - タスクの並び替えをドラッグ&ドロップで行い、変更後に連番のorderIndexを再採番する処理を用意する
  - バリデーションはTaskエンティティの責務ではあるが、保存処理が遠いため、UIでも行う
  - TripManagementでタスク読み込み/保存のI/O責務を担い、TripEntryとTaskの相互変換・永続化を行う（ユースケース経由のTaskQueryService/TripEntryRepository）
  - 並び替えは親タスクは親タスク間で、子タスクは子タスク間で行えるようにする（親タスクと子タスクでorderIndex重複可）
  - 子タスクがある場合、親タスクの削除ボタンを非表示にする
  - 親タスクに完了チェックをオンにしたら自動的にすべての子タスクを完了にする
  - 親タスクが完了かつ子タスクが未完了の場合はエラーとする
- タスクの複製を可能にする
  - タスク編集画面にコピーアイコンとペーストアイコンを置く
  - コピー実施で、対象タスクのtripIdを内部保持
  - tripIdを内部保持しているときだけペーストアイコンが使用可能
  - ペースト実施で、tripIdに紐づくタスクを取得し、画面上のタスクと置き換える(すでにタスクがある場合はすべて削除して置き換える)
  - ペースト時にtripIdは現在編集中のtripIdへ更新する
  - タスクのIdは再生成し、親タスクIdもそれに合わせて更新する
  - ペーストは画面値を置き換えることになるため、確認ダイアログを表示する
  - 旅行の新規作成時はコピーアイコン使用不可

## 地図画面

- ピンタップ時にモーダルを表示するのを廃止しボトムシートに変更する
- 入力域やボタンの見た目がtrip_edit_modalと異なるので、trip_edit_modal側に合わせる
- GoogleMapMarkerManagerを廃止してMapDisplayをStatelessWidgetに変更する
- 親ウィジェットがデータ読み書き責務を持つ
- 取得した現在地情報を保持しておき、地図ウィジェットを再度生成した時の初期値とする
- onMarkerSaveからonMarkerSavedをpinをパラメータとして呼び出す
- pinデータが存在する場合、地図の初期位置は最初のpin位置とする
- ボトムシートを開いた状態で別のピンをタップすると、ボトムシートの内容が更新されるようにする

## マップピンボトムシート

- 訪問開始日と訪問終了日は時分まで入力できるようにする
- 保存ボタンタップで呼び出し元にコールバックする
- Pinデータを受け取り、各項目に初期セットする
- 保存ボタン処理
  - タップ時に訪問開始日時が訪問終了日時より後の場合にエラーメッセージを表示する
  - Pinデータを作成し、呼び出し元にコールバックする
- 上部にpinの位置情報からGoogle Places APIで場所名を取得して表示する
  - 場所名がブランクの場合のみ、Google Places APIで場所名を取得する
  - 場所名のボックス右端に更新アイコンを配置し、タップでGoogle Places APIで場所名を再取得する
  - 場所名も更新処理時に保存する

## 招待機能

- MemberInvitationエンティティを作成する
- メンバー編集画面に(新規の時は除く)招待ボタンを配置する
- 招待ボタンボタンクリックでMemberInvitationを作成
  - 招待メンバーIDをinviteeId、ログインユーザーのメンバーIDをinviterIdとする
  - invitationCodeはUUIDで生成
  - 招待メンバーIDでinviteeIdを紐づけて、存在する場合は更新、存在しない場合は新規作成
- ログイン時の処理改修
  - GetOrCreateMemberUseCaseの処理を廃止
  - ログイン時に、ユーザーIDでメンバーのAccountIdを紐づけて、存在チェック
  - 存在しない場合にダイアログを表示し、新規作成or招待コード入力を選択
    - 新規作成の場合は、displayName: user.loginId, accountId: user.id, email: user.loginId,でメンバー作成
    - 招待コード入力の場合は、入力されたコードでMemberInvitationを紐づけ、accountIdにuser.idをセットして更新する
    - 入力された招待コードで紐づかない場合はエラー表示（新規作成はしない。）
- 招待受諾時、inviteeIdでmembers.idを取得し、accountIdがnullでない場合は無効と判定して更新しない
  - この仕様を廃止する（accountIdがnullでない場合でも更新する）

## グループイベント


## DVCポイント計算画面

- サイドメニューに「DVCポイント計算」を追加する
  - `NavigationItem`にDVCポイント計算を追加する
  - メニュー選択時にDVCポイント計算用の画面フローへ遷移できるようにする
- DVCポイント計算画面の遷移を「グループ一覧 → 計算画面」にする
  - サイドメニューからDVCポイント計算を開いた初期表示はグループ一覧にする
  - グループ一覧でグループを選択すると、選択グループのDVCポイント計算画面を表示する
  - 計算画面の戻る操作でグループ一覧に戻れるようにする
- グループ一覧を年表画面とDVCポイント計算画面で共通化する
  - 年表用のグループ一覧表示ロジックを共通コンポーネント化する
  - 遷移先（年表 / DVCポイント計算）をコールバックで切り替えられる設計にする
  - 共通化後も年表画面の既存挙動を維持する

## デザイン

- ベースカラーを青系統に変更する

## 全体

- 画面の一部にポップアップして表示する画面のファイル名の末尾は"dialog"ではなく"modal"で統一する
- エンティティの設計をuser.dartに合わせる
- DatePickerの操作性改善（日付タップで直接確定）
  - DatePickerダイアログ上部の年月日表記に曜日も表示する
  - DatePickerダイアログ上部の年月日表記タップで直接入力で年月日を変更できるようにする
- _showDeleteConfirmDialog（削除ダイアログ）の共通ウィジェット化
- 編集画面の変更状態をRiverpodで管理し、キャンセル時に確認ダイアログを表示する
  - 状態管理の作成
  - グループ編集画面に適用
  - メンバー編集画面に適用
  - 旅行編集画面に適用

## リファクタリング

- GroupDtoを作成する
  - GroupMemberDtoとGroupEventDtoも作成し、GroupDtoにリストとして保持する
  - マッパーも用意する
- Repository Factoryパターンを導入してDB切り替えを可能にする
  - DatabaseTypeのenumとRiverpod Providerを作成
  - RepositoryFactoryを実装
- QueryService Factoryパターンを導入してDB切り替えを可能にする
  - QueryServiceFactoryを実装
- TopPageにUseCaseを渡さない
- GroupListにUseCaseを渡さない
- AuthService Factoryパターンを導入してサービスを切り替えを可能にする
  - AuthTypeのenumとRiverpod Providerを作成
  - AuthServiceFactoryを実装
- UseCaseをProviderに依存する形に変更する
  - GroupManagementで実装
- UseCase Providerを利用するようにリポジトリDIをリファクタリングする
  - 全機能に展開する
- 以下、StateNotifierになっている箇所をNotifierに変更する
  - AuthNotifier
  - LocationNotifier
  - NavigationNotifier
  - GroupTimelineNavigationNotifier
- GroupTimelineのConsumerStatefulWidgetを廃止し、HookConsumerWidgetにする
- route_info_view.dartをroute_info_view.dart,route_list.dart,route_map.dartに分割する
- RouteList/RouteMapウィジェット側に関連ロジックも集約する
- 経路情報の取得処理をusecaseに切り出す（RouteInfoViewのsearchRoutesロジック）
- RouteInfoViewでのGoogleRoutesApiRouteInfoService直接生成を廃止し、usecase経由で取得する（Env参照含む）
- 以下の画面のStatefulWidget(またはConsumerStatefulWidget)をHookWidget(またはHookConsumerWidget)に変更する
  - TopPage
  - AccountDeleteModal
  - EmailChangeModal
  - PasswordChangeModal
  - ReauthenticateModal
  - LoginPage
  - SignUpPage
  - GroupEditModal
  - GroupManagement
  - MapScreen
  - MemberEditModal
  - MemberManagement
  - TripEditModal
  - TripManagement
  - GroupList
  - CustomDatePickerDialog
  - InvitationCodeModal
  - CustomSearchBar
  - GoogleMapView
  - PinDetailBottomSheet
  - RouteMemoEditBottomSheet
  - RouteInfoView
- TopPageで取得しているcurrentMemberをRiverpodでグローバル管理する
  - currentMemberの取得責務をNotifier/Providerに移す
  - 各画面へのmember引数受け渡しを廃止する
- TaskViewのbuildParentCardを分割する
- pin_detailsを廃止する
- routesを廃止する（ER図、エンティティ、リポジトリなどを整理する）

## 不具合修正

- グループ管理画面とメンバー管理画面で、編集後に一覧画面に戻った際、一覧が最新状態に更新されず、再度編集を開くと古いデータのままになっている不具合を修正する
  - グループ管理画面
  - メンバー管理画面
  - グループ編集画面で、メモ欄を空欄にして更新して再度開くと元に戻っている（空欄が反映されない）不具合を修正する
- 旅行編集でメモを空白で更新するとnullになる不具合を修正する（グループのメモと同じく空欄保持）
