---
name: independent-review
description: プルリクエストの作成・更新・レビュー返信前に、今回の更新差分にドキュメント以外が含まれるかで独立レビューの要否を判定し、必要な場合にPR全体と関連箇所までサブエージェントでレビューするスキル。
---

# Independent Review

今回の更新差分の内容で独立レビューの要否を判定し、レビューが必要な場合は差分だけに限定せず影響範囲まで確認する。

## 適用判定

- 今回の更新差分にドキュメント以外の変更が含まれる場合は、独立レビューを必須とする。
- 今回の更新差分がない場合、またはドキュメントのみの場合は、独立レビューを不要とする。
- プルリクエストの作成、既存プルリクエストへのpush・本文更新、レビューコメントへの返信のすべてに同じ判定を適用する。操作の種類、返信内容、ユーザーが対応方針を指定したかどうかで例外を作らない。
- ファイル種別は拡張子だけでなく役割で判断する。ビルドスクリプト、CI定義、依存・環境設定、生成物、テストはドキュメント以外として扱い、Markdownで記載された手順・設計・スキルはドキュメントとして扱う。

今回の更新差分は操作ごとに次の範囲とする。

- 新規プルリクエスト作成: 比較元ブランチからHEADまでの差分全体
- 既存プルリクエストへのpush: 前回push済みのHEADから今回pushするHEADまでの差分
- 既存プルリクエストの本文等の更新: 今回同時に反映するファイル差分。ファイル変更がなければ差分なし
- レビューコメントへの返信: その指摘への対応として今回追加したファイル差分。修正しない返信なら差分なし

## レビュー範囲

独立レビューが必要な場合、レビュー対象を今回の更新差分だけに限定しない。少なくとも次を確認する。

- 今回の更新差分
- 比較元ブランチからのプルリクエスト差分全体と変更目的
- 変更箇所の呼び出し元、利用先、依存関係、状態遷移
- 同じ責務・API・制御構造を持つ関連・同等ケース
- 仕様、実装、テスト、ドキュメント間の整合と、変更が及ぼす影響範囲

## 独立レビュー

独立レビューが必要な場合は、今回の更新差分、プルリクエスト全体の変更目的、関連経路をサブエージェントへ渡し、次を依頼する。

- `AGENTS.md`のレビュー方針、アーキテクチャ、コーディング規約との整合を確認する。
- 実際の発生条件と影響を確認し、ファイルを変更せず日本語で報告する。
- 差分だけを読むのではなく、変更箇所の呼び出し元・利用先と関連・同等ケースをたどり、影響範囲を確認する。

レビュー結果は、指摘の有無、発生条件、影響、確認した関連・同等ケースとともに呼び出し元へ返す。このスキルは指摘対応や再レビューを起動しない。妥当な指摘への対応には、呼び出し元が`review-finding-handler`を適用し、修正・検証・コミット後に本スキルを再度適用する。

独立レビューが必要なのにサブエージェントを利用できない場合は、その状況を呼び出し元へ報告する。呼び出し元は、独立レビューを実施できない場合や妥当な指摘が未解決の場合、予定していたプルリクエストの作成・更新・push・返信を行わない。
