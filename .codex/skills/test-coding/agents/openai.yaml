interface:
  display_name: "Test Coding"
  short_description: "このリポジトリでテストコードを扱うときに必ず使うテストスキル"
  default_prompt: "Always use $test-coding when adding, updating, or refactoring tests in this repository. Follow the repository's test conventions and decide the test structure before entering the Red step of TDD."
policy:
  allow_implicit_invocation: true
