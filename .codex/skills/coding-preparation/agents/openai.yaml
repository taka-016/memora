interface:
  display_name: "Coding Preparation"
  short_description: "このリポジトリでコード変更を始める前に必ず使う準備スキル"
  default_prompt: "Always use $coding-preparation before starting code changes in this repository. Prepare the task by checking todo handling, creating a work branch when needed, inspecting the `lib` and `test` tree, and reviewing the design docs before editing files."
policy:
  allow_implicit_invocation: true
