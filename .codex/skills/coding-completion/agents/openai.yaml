interface:
  display_name: "Coding Completion"
  short_description: "このリポジトリでコード変更を完了させるときに必ず使う締めのスキル"
  default_prompt: "Use $coding-completion to complete checks and commits, independently review changes, handle valid findings, repeat review until clear, then push, create a PR, or reply to review comments."
policy:
  allow_implicit_invocation: true
