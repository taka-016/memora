interface:
  display_name: "Coding Completion"
  short_description: "このリポジトリでコード変更を完了させるときに必ず使う締めのスキル"
  default_prompt: "Always use $coding-completion before finishing code changes in this repository. Complete the required checks and commits, independently review the changes, then push or create a PR or reply to review comments."
policy:
  allow_implicit_invocation: true
