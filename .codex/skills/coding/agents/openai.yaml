interface:
  display_name: "Coding"
  short_description: "このリポジトリでコード変更を行うときに必ず使うコーディングスキル"
  default_prompt: "Always use $coding when making code changes in this repository. Implement the change while following the presentation-layer dependency rules, the TDD workflow, the spec-escalation rule, and the refactoring policy."
policy:
  allow_implicit_invocation: true
