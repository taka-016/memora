# ユーザーストーリー

## 【認証・セキュリティ】

### 安全なアクセス
**ストーリー**: ユーザーとして、安全にアプリケーションにアクセスしたい。大切な家族の記録を守りたいから。

**受け入れ基準**:
```gherkin
Scenario: Google認証でログイン
  Given ユーザーがログイン画面にアクセスしている
  When ユーザーがGoogle認証ボタンをタップする
  Then Googleの認証画面が表示される
  When ユーザーが有効なGoogleアカウントで認証する
  Then ユーザーはトップページにリダイレクトされる
  And ログイン状態が保持される

Scenario: メールアドレスとパスワードでログイン
  Given ユーザーがログイン画面にアクセスしている
  When ユーザーが有効なメールアドレスとパスワードを入力する
  And ログインボタンをタップする
  Then ユーザーはトップページにリダイレクトされる
  And ログイン状態が保持される

Scenario: 無効な認証情報でログイン失敗
  Given ユーザーがログイン画面にアクセスしている
  When ユーザーが無効なメールアドレスまたはパスワードを入力する
  And ログインボタンをタップする
  Then エラーメッセージ「認証情報が正しくありません」が表示される
  And ログイン画面にとどまる

Scenario: パスワードポリシーの検証
  Given ユーザーがアカウント登録画面にアクセスしている
  When ユーザーが「password」のような弱いパスワードを入力する
  Then エラーメッセージ「パスワードは大文字、小文字、特殊文字、数字を含む8文字以上で入力してください」が表示される
  When ユーザーが「Password123!」のような強いパスワードを入力する
  Then エラーメッセージが消える

Scenario: ログアウト
  Given ユーザーがログイン済みの状態でアプリにアクセスしている
  When ユーザーがハンバーガーメニューからログアウトを選択する
  Then ユーザーはログイン画面にリダイレクトされる
  And ログイン状態が解除される
```

### 招待機能
**ストーリー**: ユーザーとして、家族や友人をアプリに招待したい。家族や友人と記録を共有したいから。

**受け入れ基準**:
```gherkin
Scenario: 招待URLの生成
  Given ユーザーがログイン済みの状態でアプリにアクセスしている
  When ユーザーが招待機能を選択する
  And 招待URL生成ボタンをタップする
  Then 招待URLが生成される
  And 招待URLが表示される
  And 招待URLの有効期限が24時間後に設定される

Scenario: 有効な招待URLでアカウント登録
  Given 招待URLが生成されている
  When 招待を受けた人が有効な招待URLにアクセスする
  Then アカウント登録画面が表示される
  When 招待を受けた人が有効な情報でアカウント登録する
  Then アカウントが作成される
  And 招待元のメンバーに自動的に紐づけられる

Scenario: 無効な招待URLでアクセス
  Given 招待URLの有効期限が切れている
  When 招待を受けた人が無効な招待URLにアクセスする
  Then エラーメッセージ「招待URLの有効期限が切れています」が表示される
  And ログイン画面にリダイレクトされる
```

## 【基本データ管理】

### グループ作成
**ストーリー**: ユーザーとして、グループを作成したい。友人や家族と記録を共有したいから。

**受け入れ基準**:
```gherkin
Scenario: 有効なグループ名でグループ作成
  Given ユーザーがログイン済みの状態でグループ作成画面にアクセスしている
  When ユーザーが「家族」というグループ名を入力する
  And 作成ボタンをタップする
  Then グループが作成される
  And 作成したグループが所属グループ一覧に表示される

Scenario: 空のグループ名でグループ作成失敗
  Given ユーザーがログイン済みの状態でグループ作成画面にアクセスしている
  When ユーザーが空のグループ名を入力する
  And 作成ボタンをタップする
  Then エラーメッセージ「グループ名は必須です」が表示される
  And グループは作成されない

Scenario: 50文字を超えるグループ名でグループ作成失敗
  Given ユーザーがログイン済みの状態でグループ作成画面にアクセスしている
  When ユーザーが51文字のグループ名を入力する
  And 作成ボタンをタップする
  Then エラーメッセージ「グループ名は50文字以内で入力してください」が表示される
  And グループは作成されない

Scenario: 重複するグループ名でグループ作成成功
  Given ユーザーがログイン済みの状態でグループ作成画面にアクセスしている
  And 既に「家族」というグループが存在している
  When ユーザーが「家族」というグループ名を入力する
  And 作成ボタンをタップする
  Then グループが作成される
  And 作成したグループが所属グループ一覧に表示される
```

### グループ管理
**ストーリー**: ユーザーとして、作成したグループを管理したい。グループ情報を最新に保ちたいから。

**受け入れ基準**:
```gherkin
Scenario: 自分が作成したグループの名前変更
  Given ユーザーがログイン済みで自分が作成したグループ「家族」が存在している
  When ユーザーがグループ「家族」の編集画面にアクセスする
  And グループ名を「家族・親戚」に変更する
  And 保存ボタンをタップする
  Then グループ名が「家族・親戚」に更新される
  And 所属グループ一覧に更新されたグループ名が表示される

Scenario: 自分が作成したグループの削除
  Given ユーザーがログイン済みで自分が作成したグループ「家族」が存在している
  When ユーザーがグループ「家族」の削除ボタンをタップする
  Then 確認ダイアログ「本当に削除しますか？」が表示される
  When ユーザーが「はい」を選択する
  Then グループ「家族」が削除される
  And 関連するイベント・旅行も削除される
  And 所属グループ一覧からグループが削除される

Scenario: 他人が作成したグループの編集試行
  Given ユーザーがログイン済みで他人が作成したグループ「友人」が存在している
  When ユーザーがグループ「友人」の詳細画面にアクセスする
  Then 編集ボタンが表示されない
  And 削除ボタンが表示されない
```

### メンバー作成
**ストーリー**: ユーザーとして、メンバーを作成したい。グループに誰が入っているかを管理したいから。

**受け入れ基準**:
```gherkin
Scenario: 有効なメンバー名でメンバー作成
  Given ユーザーがログイン済みの状態でメンバー作成画面にアクセスしている
  When ユーザーが「太郎」というメンバー名を入力する
  And 作成ボタンをタップする
  Then メンバーが作成される
  And 作成したメンバーがメンバー一覧に表示される

Scenario: 作成したメンバーをグループに追加
  Given ユーザーがログイン済みで「太郎」というメンバーが存在している
  And 「家族」というグループが存在している
  When ユーザーがグループ「家族」のメンバー追加画面にアクセスする
  And メンバー「太郎」を選択する
  And 追加ボタンをタップする
  Then メンバー「太郎」がグループ「家族」に追加される

Scenario: 1つのメンバーを複数のグループに追加
  Given ユーザーがログイン済みで「太郎」というメンバーが存在している
  And 「家族」と「友人」というグループが存在している
  When ユーザーがメンバー「太郎」をグループ「家族」に追加する
  And メンバー「太郎」をグループ「友人」に追加する
  Then メンバー「太郎」が両方のグループに所属する

Scenario: 空のメンバー名でメンバー作成失敗
  Given ユーザーがログイン済みの状態でメンバー作成画面にアクセスしている
  When ユーザーが空のメンバー名を入力する
  And 作成ボタンをタップする
  Then エラーメッセージ「メンバー名は必須です」が表示される
  And メンバーは作成されない

Scenario: 30文字を超えるメンバー名でメンバー作成失敗
  Given ユーザーがログイン済みの状態でメンバー作成画面にアクセスしている
  When ユーザーが31文字のメンバー名を入力する
  And 作成ボタンをタップする
  Then エラーメッセージ「メンバー名は30文字以内で入力してください」が表示される
  And メンバーは作成されない
```

### メンバー管理
**ストーリー**: ユーザーとして、作成したメンバーを管理したい。メンバー情報を最新に保ちたいから。

**受け入れ基準**:
```gherkin
Scenario: 自分が作成したメンバーの名前変更
  Given ユーザーがログイン済みで自分が作成したメンバー「太郎」が存在している
  When ユーザーがメンバー「太郎」の編集画面にアクセスする
  And メンバー名を「太郎くん」に変更する
  And 保存ボタンをタップする
  Then メンバー名が「太郎くん」に更新される
  And メンバー一覧に更新されたメンバー名が表示される

Scenario: 自分が作成したメンバーの削除
  Given ユーザーがログイン済みで自分が作成したメンバー「太郎」が存在している
  When ユーザーがメンバー「太郎」の削除ボタンをタップする
  Then 確認ダイアログ「本当に削除しますか？」が表示される
  When ユーザーが「はい」を選択する
  Then メンバー「太郎」が削除される
  And 関連するイベント・旅行も削除される
  And メンバー一覧からメンバーが削除される

Scenario: 他人が作成したメンバーの編集試行
  Given ユーザーがログイン済みで他人が作成したメンバー「花子」が存在している
  When ユーザーがメンバー「花子」の詳細画面にアクセスする
  Then 編集ボタンが表示されない
  And 削除ボタンが表示されない
```

### 自分のグループ確認
**ストーリー**: ユーザーとして、自分が入っているグループを知りたい。グループは自分が作らないこともあるから。

**受け入れ基準**:
```gherkin
Scenario: 所属グループの一覧表示
  Given ユーザーがログイン済みの状態でアプリにアクセスしている
  And ユーザーが「家族」と「友人」というグループに所属している
  When ユーザーがグループ一覧画面にアクセスする
  Then 所属グループ「家族」と「友人」が表示される
  And 各グループの名前と作成者が表示される
  And 作成日時順で並び替えされている

Scenario: 所属グループが存在しない場合
  Given ユーザーがログイン済みの状態でアプリにアクセスしている
  And ユーザーがどのグループにも所属していない
  When ユーザーがグループ一覧画面にアクセスする
  Then 「所属しているグループはありません」というメッセージが表示される
```

## 【イベント管理】

### イベントの登録
**ストーリー**: ユーザーとして、年表上でイベントを登録したい。時系列で整理して記録したいから。

**受け入れ基準**:
```gherkin
Scenario: 有効なイベント情報でイベント登録
  Given ユーザーがログイン済みの状態で年表画面にアクセスしている
  When ユーザーが年表のイベント項目をタップする
  Then イベント入力画面が表示される
  When ユーザーがイベント名「誕生日」、日付「2024-01-01」、メモ「太郎の誕生日」を入力する
  And 個人のイベントを選択する
  And 保存ボタンをタップする
  Then イベントが登録される
  And 年表にイベントが表示される

Scenario: 必須項目未入力でイベント登録失敗
  Given ユーザーがログイン済みの状態でイベント入力画面にアクセスしている
  When ユーザーがイベント名を空にして保存ボタンをタップする
  Then エラーメッセージ「イベント名は必須です」が表示される
  And イベントは登録されない

Scenario: 100文字を超えるイベント名でイベント登録失敗
  Given ユーザーがログイン済みの状態でイベント入力画面にアクセスしている
  When ユーザーが101文字のイベント名を入力する
  And 保存ボタンをタップする
  Then エラーメッセージ「イベント名は100文字以内で入力してください」が表示される
  And イベントは登録されない

Scenario: グループのイベント登録
  Given ユーザーがログイン済みの状態でイベント入力画面にアクセスしている
  When ユーザーがイベント名「新年会」、日付「2024-01-01」を入力する
  And グループのイベントを選択する
  And 保存ボタンをタップする
  Then グループのイベントが登録される
  And 年表にグループのイベントが表示される
```

### イベントの編集・削除
**ストーリー**: ユーザーとして、登録したイベントを編集・削除したい。間違いを修正したり、不要になったイベントを削除したいから。

**受け入れ基準**:
```gherkin
Scenario: 自分が作成したイベントの編集
  Given ユーザーがログイン済みで自分が作成したイベント「誕生日」が存在している
  When ユーザーがイベント「誕生日」の編集画面にアクセスする
  And イベント名を「太郎の誕生日」に変更する
  And 保存ボタンをタップする
  Then イベントが更新される
  And 年表に更新されたイベントが表示される

Scenario: 自分が作成したイベントの削除
  Given ユーザーがログイン済みで自分が作成したイベント「誕生日」が存在している
  When ユーザーがイベント「誕生日」の削除ボタンをタップする
  Then 確認ダイアログ「本当に削除しますか？」が表示される
  When ユーザーが「はい」を選択する
  Then イベント「誕生日」が削除される
  And 年表からイベントが削除される

Scenario: 他人が作成したイベントの編集試行
  Given ユーザーがログイン済みで他人が作成したイベント「結婚式」が存在している
  When ユーザーがイベント「結婚式」の詳細画面にアクセスする
  Then 編集ボタンが表示されない
  And 削除ボタンが表示されない
```

## 【旅行管理】

### 旅行の登録
**ストーリー**: ユーザーとして、年表上で旅行を登録したい。時系列で整理して記録したいから。

**受け入れ基準**:
```gherkin
Scenario: 有効な旅行情報で旅行登録
  Given ユーザーがログイン済みの状態で年表画面にアクセスしている
  When ユーザーが年表の旅行項目をタップする
  Then 旅行入力画面が表示される
  When ユーザーが旅行名「沖縄旅行」、開始日「2024-01-01」、終了日「2024-01-03」、メモ「家族旅行」を入力する
  And 旅行参加者を選択する
  And 保存ボタンをタップする
  Then 旅行が登録される
  And 年表に旅行が表示される

Scenario: 必須項目未入力で旅行登録失敗
  Given ユーザーがログイン済みの状態で旅行入力画面にアクセスしている
  When ユーザーが旅行名を空にして保存ボタンをタップする
  Then エラーメッセージ「旅行名は必須です」が表示される
  And 旅行は登録されない

Scenario: 開始日が終了日より後の場合の旅行登録失敗
  Given ユーザーがログイン済みの状態で旅行入力画面にアクセスしている
  When ユーザーが旅行名「沖縄旅行」、開始日「2024-01-03」、終了日「2024-01-01」を入力する
  And 保存ボタンをタップする
  Then エラーメッセージ「開始日は終了日より前の日付を入力してください」が表示される
  And 旅行は登録されない

Scenario: 100文字を超える旅行名で旅行登録失敗
  Given ユーザーがログイン済みの状態で旅行入力画面にアクセスしている
  When ユーザーが101文字の旅行名を入力する
  And 保存ボタンをタップする
  Then エラーメッセージ「旅行名は100文字以内で入力してください」が表示される
  And 旅行は登録されない
```

### 旅行の編集・削除
**ストーリー**: ユーザーとして、登録した旅行を編集・削除したい。間違いを修正したり、不要になった旅行を削除したいから。

**受け入れ基準**:
```gherkin
Scenario: 自分が作成した旅行の編集
  Given ユーザーがログイン済みで自分が作成した旅行「沖縄旅行」が存在している
  When ユーザーが旅行「沖縄旅行」の編集画面にアクセスする
  And 旅行名を「沖縄家族旅行」に変更する
  And 保存ボタンをタップする
  Then 旅行が更新される
  And 年表に更新された旅行が表示される

Scenario: 自分が作成した旅行の削除
  Given ユーザーがログイン済みで自分が作成した旅行「沖縄旅行」が存在している
  When ユーザーが旅行「沖縄旅行」の削除ボタンをタップする
  Then 確認ダイアログ「本当に削除しますか？」が表示される
  When ユーザーが「はい」を選択する
  Then 旅行「沖縄旅行」が削除される
  And 年表から旅行が削除される

Scenario: 他人が作成した旅行の編集試行
  Given ユーザーがログイン済みで他人が作成した旅行「北海道旅行」が存在している
  When ユーザーが旅行「北海道旅行」の詳細画面にアクセスする
  Then 編集ボタンが表示されない
  And 削除ボタンが表示されない
```

### 旅行場所の記録
**ストーリー**: ユーザーとして、旅行した場所を記録したい。後でその場所を思い出したいから。

**受け入れ基準**:
```gherkin
Scenario: 旅行に位置情報を追加
  Given ユーザーがログイン済みで旅行「沖縄旅行」が存在している
  When ユーザーが旅行「沖縄旅行」の詳細画面にアクセスする
  And 位置情報追加ボタンをタップする
  Then 位置情報検索画面が表示される
  When ユーザーが「首里城」で検索する
  And 検索結果から「首里城」を選択する
  And 訪問期間「2024-01-01」から「2024-01-01」を設定する
  And メモ「歴史を感じる場所」を入力する
  And 保存ボタンをタップする
  Then 位置情報が旅行に追加される

Scenario: 複数の位置情報を1つの旅行に追加
  Given ユーザーがログイン済みで旅行「沖縄旅行」が存在している
  When ユーザーが旅行「沖縄旅行」に「首里城」の位置情報を追加する
  And 旅行「沖縄旅行」に「美ら海水族館」の位置情報を追加する
  Then 両方の位置情報が旅行に紐づけられる
  And 詳細画面で両方の位置情報が表示される

Scenario: 500文字を超えるメモで位置情報追加失敗
  Given ユーザーがログイン済みで位置情報追加画面にアクセスしている
  When ユーザーが501文字のメモを入力する
  And 保存ボタンをタップする
  Then エラーメッセージ「メモは500文字以内で入力してください」が表示される
  And 位置情報は追加されない
```

## 【表示・UI】

### グループ年表の表示
**ストーリー**: ユーザーとして、年表形式でグループのイベントを見たい。時間の流れに沿ってグループの記録を整理したいから。

**受け入れ基準**:
```gherkin
Scenario: 選択したグループの年表表示
  Given ユーザーがログイン済みでグループ「家族」に所属している
  And グループ「家族」にイベントと旅行が登録されている
  When ユーザーがグループ「家族」の年表画面にアクセスする
  Then グループ「家族」の年表が表示される
  And グループのイベントと旅行が時系列で表示される
  And 個人のイベントも同じ年表に表示される
  And 年単位で分割されている

Scenario: 過去・現在・未来の年の表示
  Given ユーザーがログイン済みで年表画面にアクセスしている
  When ユーザーが年表を確認する
  Then 過去の年が表示される
  And 現在の年が表示される
  And 未来の年が表示される

Scenario: イベントと旅行の視覚的区別
  Given ユーザーがログイン済みで年表画面にアクセスしている
  And イベントと旅行が年表に表示されている
  When ユーザーが年表を確認する
  Then イベントと旅行が異なるアイコンで表示される
  And イベントと旅行が異なる色で表示される
```

### 旅行先の地図表示
**ストーリー**: ユーザーとして、旅行先を地図で確認したい。場所を視覚的に把握したいから。

**受け入れ基準**:
```gherkin
Scenario: 地図上で旅行先の位置表示
  Given ユーザーがログイン済みで旅行「沖縄旅行」が存在している
  And 旅行「沖縄旅行」に「首里城」の位置情報が追加されている
  When ユーザーが地図画面にアクセスする
  Then 地図上で「首里城」の位置がピンで表示される

Scenario: ピンタップで旅行詳細情報表示
  Given ユーザーがログイン済みで地図画面にアクセスしている
  And 地図上に「首里城」のピンが表示されている
  When ユーザーが「首里城」のピンをタップする
  Then 旅行「沖縄旅行」の詳細情報が表示される
  And 訪問期間とメモが表示される

Scenario: 過去の旅行先の地図表示
  Given ユーザーがログイン済みで複数の旅行が存在している
  And 各旅行に位置情報が追加されている
  When ユーザーが地図画面にアクセスする
  Then 過去の旅行先がすべて地図上に表示される
  And 各旅行先がピンで表示される

Scenario: 地図の操作
  Given ユーザーがログイン済みで地図画面にアクセスしている
  When ユーザーが地図をピンチする
  Then 地図が拡大・縮小される
  When ユーザーが地図をドラッグする
  Then 地図が移動される

Scenario: 位置情報の検索
  Given ユーザーがログイン済みで地図画面にアクセスしている
  When ユーザーが検索ボックスに「首里城」と入力する
  And 検索ボタンをタップする
  Then 検索結果が地図上に表示される
  And 該当する位置にピンが表示される
```

### 同じ場所の記録参照
**ストーリー**: ユーザーとして、同じ場所の過去の記録を見たい。再訪時の参考にしたいから。

**受け入れ基準**:
```gherkin
Scenario: 同じ場所の過去の記録表示
  Given ユーザーがログイン済みで「首里城」の位置情報が複数の旅行に追加されている
  When ユーザーが「首里城」の詳細画面にアクセスする
  Then 「首里城」を訪問した過去の旅行が一覧表示される
  And 各旅行の訪問日時とメモが表示される
  And 時系列順で表示される

Scenario: 同じ場所の記録が存在しない場合
  Given ユーザーがログイン済みで「美ら海水族館」の位置情報が1回だけ追加されている
  When ユーザーが「美ら海水族館」の詳細画面にアクセスする
  Then 「過去の記録はありません」というメッセージが表示される
```

### 直感的なUI操作
**ストーリー**: ユーザーとして、直感的なUIで操作したい。記録作成に集中して、思い出を大切にしたいから。

**受け入れ基準**:
```gherkin
Scenario: ハンバーガーメニューでの機能アクセス
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーがハンバーガーメニューをタップする
  Then メニューが表示される
  And 「トップページ」「グループ年表」「マップ表示」「グループ設定」「メンバー設定」「アカウント設定」の項目が表示される
  When ユーザーがメニュー項目をタップする
  Then 対応する画面に遷移される

Scenario: トップページの年表表示
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーがトップページにアクセスする
  Then 年表画面が表示される
  And 所属グループのイベントと旅行が表示される

Scenario: 画面遷移の分かりやすさ
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーが各画面を遷移する
  Then 現在の画面が分かるようにタイトルが表示される
  And 戻るボタンが適切に表示される

Scenario: ローディング状態の表示
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーが時間のかかる操作を実行する
  Then ローディングインジケーターが表示される
  And 操作完了後にローディングインジケーターが消える

Scenario: エラーメッセージの分かりやすさ
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーが無効な操作を実行する
  Then 具体的で分かりやすいエラーメッセージが表示される
  And エラーの解決方法が示される
```

## 【追加機能】

### エラーハンドリング
**ストーリー**: ユーザーとして、エラーが発生した時に適切に対処したい。アプリを安心して使い続けたいから。

**受け入れ基準**:
```gherkin
Scenario: ネットワークエラー時の再試行
  Given ユーザーがログイン済みでアプリにアクセスしている
  And ネットワークに接続されていない状態である
  When ユーザーがデータを取得しようとする
  Then エラーメッセージ「ネットワークに接続されていません」が表示される
  And 再試行ボタンが表示される
  When ユーザーが再試行ボタンをタップする
  And ネットワークが復旧している
  Then データの取得が再実行される

Scenario: 無効なデータ入力時のエラー
  Given ユーザーがログイン済みでデータ入力画面にアクセスしている
  When ユーザーが無効なデータを入力する
  Then 具体的なエラーメッセージが表示される
  And 入力フィールドがハイライトされる
  And 正しい入力方法が示される

Scenario: 権限エラー時の説明
  Given ユーザーがログイン済みでアプリにアクセスしている
  When ユーザーが権限のない操作を実行しようとする
  Then エラーメッセージ「この操作を実行する権限がありません」が表示される
  And 権限を得る方法が説明される

Scenario: アプリクラッシュ時の自動復旧
  Given ユーザーがログイン済みでアプリを使用している
  When アプリがクラッシュする
  Then アプリが自動的に再起動される
  And ユーザーの作業中のデータが可能な限り復旧される
  And クラッシュの原因が記録される
```

### データの検索・フィルタリング
**ストーリー**: ユーザーとして、大量のデータから特定の情報を素早く見つけたい。効率的に記録を管理したいから。

**受け入れ基準**:
```gherkin
Scenario: イベント名による検索
  Given ユーザーがログイン済みで年表画面にアクセスしている
  And 複数のイベントが登録されている
  When ユーザーが検索ボックスに「誕生日」と入力する
  Then 「誕生日」を含むイベントが検索結果に表示される
  And 検索結果で「誕生日」がハイライト表示される

Scenario: 旅行名による検索
  Given ユーザーがログイン済みで年表画面にアクセスしている
  And 複数の旅行が登録されている
  When ユーザーが検索ボックスに「沖縄」と入力する
  Then 「沖縄」を含む旅行が検索結果に表示される
  And 検索結果で「沖縄」がハイライト表示される

Scenario: 日付範囲でのフィルタリング
  Given ユーザーがログイン済みで年表画面にアクセスしている
  And 複数の年にまたがるイベントと旅行が登録されている
  When ユーザーが「2024年1月1日」から「2024年12月31日」の範囲を選択する
  Then 2024年のイベントと旅行のみが表示される
  And 他の年のイベントと旅行は非表示になる

Scenario: 参加者でのフィルタリング
  Given ユーザーがログイン済みで年表画面にアクセスしている
  And 複数のメンバーが参加するイベントと旅行が登録されている
  When ユーザーが「太郎」を参加者フィルターで選択する
  Then 「太郎」が参加するイベントと旅行のみが表示される
  And 他のメンバーのみのイベントと旅行は非表示になる

Scenario: 検索結果が存在しない場合
  Given ユーザーがログイン済みで年表画面にアクセスしている
  When ユーザーが存在しないキーワードで検索する
  Then 「検索結果が見つかりません」というメッセージが表示される
  And 検索条件を変更する提案が表示される
```